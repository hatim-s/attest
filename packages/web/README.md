# Dashboard SPA

The React/Vite dashboard is compiled to one self-contained HTML document. The build then wraps
that document as the `@attest/web/embedded` module so the CLI can serve it without a frontend
process or network dependency.
