{
  "name": "@attest/conformance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:fuzz": "FUZZ=1 vitest run src/fuzz",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@attest/contracts": "workspace:*",
    "fast-check": "^4"
  }
}
