{
  "description": "Accepts a well-formed agent-reported failure as a result rather than an invocation error.",
  "expect": "valid",
  "input": {
    "protocol": "attest.agent-invocation",
    "error": { "message": "The upstream service timed out.", "code": "UPSTREAM_TIMEOUT" }
  }
}
