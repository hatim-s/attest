{
  "description": "Accepts an opaque state token on a successful response.",
  "expect": "valid",
  "input": {
    "protocol": "attest.agent/v1alpha1",
    "output": "Paris",
    "state": { "cursor": 1, "opaque": ["next-turn"] }
  }
}
