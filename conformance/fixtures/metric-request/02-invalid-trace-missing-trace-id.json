{
  "description": "Rejects a supplied trace missing its required trace_id instead of treating it as null.",
  "expect": "invalid",
  "issue_paths": [["trace", "trace_id"]],
  "input": {
    "protocol": "attest.metric/v1alpha1",
    "case": { "id": "greeting", "input": {} },
    "output": "Paris",
    "trace": { "schema": "attest.trace/v1alpha1", "spans": [] }
  }
}
