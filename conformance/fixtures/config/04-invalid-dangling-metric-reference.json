{
  "description": "Rejects a suite reference to a metric definition that does not exist.",
  "expect": "invalid",
  "issue_paths": [["suites", "0", "metrics", "0"]],
  "input": {
    "config_version": 1,
    "agent": { "type": "cli", "command": ["bun", "run", "agent.ts"] },
    "suites": [
      {
        "name": "smoke",
        "metrics": ["missing-metric"],
        "cases": [{ "id": "greeting", "input": {} }]
      }
    ],
    "metrics": []
  }
}
