{
  "description": "Rejects an assertion metric with no checks because vacuous metrics are forbidden.",
  "expect": "invalid",
  "issue_paths": [["metrics", "0", "assert"]],
  "input": {
    "config_version": 1,
    "agent": { "type": "cli", "command": ["bun", "run", "agent.ts"] },
    "suites": [
      {
        "name": "smoke",
        "metrics": ["empty"],
        "cases": [{ "id": "greeting", "input": {} }]
      }
    ],
    "metrics": [{ "name": "empty", "type": "assertion", "assert": [] }]
  }
}
