{
  "description": "Rejects unknown top-level fields because configs are strict documents.",
  "expect": "invalid",
  "issue_paths": [["unexpected"]],
  "input": {
    "config_version": 1,
    "unexpected": true,
    "agent": { "type": "cli", "command": ["bun", "run", "agent.ts"] },
    "suites": [
      {
        "name": "smoke",
        "metrics": ["output-exists"],
        "cases": [{ "id": "greeting", "input": {} }]
      }
    ],
    "metrics": [
      {
        "name": "output-exists",
        "type": "assertion",
        "assert": [{ "exists": { "path": "$.output" } }]
      }
    ]
  }
}
